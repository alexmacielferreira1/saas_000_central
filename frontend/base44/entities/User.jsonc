{
  "properties": {
    "organization_id": {
      "type": "string",
      "title": "Organization (tenant) ativa",
      "default": "default"
    },
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "user"
      ]
    }
  },
  "required": [
    "role"
  ],
  "name": "User",
  "type": "object"
}